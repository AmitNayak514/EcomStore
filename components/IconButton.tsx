import { Button } from "./ui/button";

const IconButton = () => {
  return (
    <Button className="rounded-full flex items-center justify-center bg-white border shadow-sm p-2 hover:scale-110 transition"></Button>
  );
};

export default IconButton;
